<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apen.onedemo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FlushActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MainActivity" />

        <activity android:name=".manager.ui.ManMainActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".manager.ui.ManGoodsActivity"/>
        <activity android:name=".manager.ui.SelectCategoryActivity"/>
        <activity android:name=".manager.ui.MaLoginActivity" />
        <activity android:name=".manager.ui.QueryGoodsActivity"/>
        <activity android:name=".manager.ui.OrderQueryActivity"/>
        <activity android:name=".manager.ui.ShopCarActivity"/>
        <activity android:name=".manager.ui.SubmitOrderActivity"/>
        <activity android:name=".manager.ui.UserInfomation"/>
        <activity android:name=".manager.ui.AddressActivity"/>
        <activity android:name=".manager.ui.CashActivity"/>
        <activity android:name=".manager.ui.ReportActivity"/>
        <activity android:name=".manager.ui.SalesActivity"/>

        <activity android:name=".manager.ui.ArticleManagerActivity"/>
        <activity android:name=".manager.ui.ArticleCategoryActivity"/>
        <activity android:name=".manager.ui.VendorManagerActivity"/>
        <activity android:name=".manager.ui.ClientManagerActivity"/>
        <activity android:name=".manager.ui.ClientStarActivity"/>


    </application>

</manifest>